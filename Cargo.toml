[workspace]
resolver = "2"
members = [
    "protocol",
    "firmware",
    "companion",
]

[patch.crates-io]
esp-hal = { path = "esp-hal-patched" }
esp-wifi = { path = "esp-wifi-patched" }

[profile.dev]
opt-level = "s"

[profile.release]
opt-level = "z"
lto = true
